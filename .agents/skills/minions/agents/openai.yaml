interface:
  display_name: "Minions"
  short_description: "Repo-specific patterns and workflows for minions"
  default_prompt: "Use the minions repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true